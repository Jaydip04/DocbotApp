<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fill color, can be a soft tertiary container or neutral color -->
    <solid android:color="?attr/colorTertiaryContainer" />

    <!-- Rounded corners -->
    <corners android:radius="12dp" />

    <!-- Optional padding around text inside the chip -->
    <padding
        android:left="12dp"
        android:top="8dp"
        android:right="12dp"
        android:bottom="8dp" />
</shape>
